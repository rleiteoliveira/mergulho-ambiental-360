# Ambiente Local de Desenvolvimento

> O guia completo de ambiente local (Windows, web demo, Unity, Docker e WSL) está em
> **[local-dev.md](local-dev.md)**. Este arquivo resume apenas o estado já configurado
> do repositório para evitar duplicação.

## Estado configurado

- Repositório Git local.
- Remoto GitHub `origin` configurado.
- Branch principal `main`.
- PoC de descoberta em branch local quando criada.
- Git LFS habilitado para assets binários comuns.
- `.gitattributes` para normalização de texto e LFS.
- `.editorconfig` para edição consistente.
- Script de verificação em `tools/check-dev-env.ps1`.

## Próximos passos

Veja **[local-dev.md](local-dev.md)** para pré-requisitos, clone, como rodar a web demo
(Python/Docker/WSL), como abrir o Unity e como validar rapidamente.
