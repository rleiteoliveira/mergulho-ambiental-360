namespace MergulhoAmbiental360.XR
{
    /*
     * XR input notes for the discovery PoC:
     *
     * - Quest 3 validation should use controller ray interaction first.
     * - Recommended Unity packages:
     *   - OpenXR
     *   - XR Plug-in Management
     *   - XR Interaction Toolkit
     *   - Meta XR SDK only when Meta-specific features are needed.
     *
     * - Main menu interaction target:
     *   - Large world-space Canvas.
     *   - XR Ray Interactor from controller.
     *   - Buttons sized for children and assisted operation by a teacher/monitor.
     *
     * - Editor fallback:
     *   - Mouse clicks for UI.
     *   - Keyboard shortcuts for player operations.
     *
     * - Hand tracking:
     *   - Do not implement in this PoC.
     *   - Keep as a future option after controller flow is validated.
     */
    public static class XRInputNotes
    {
    }
}
